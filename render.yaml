services:
  - type: web
    name: roshi-fit-club
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    routes:
      - type: rewrite
        source: /*
        destination: /index.html